<div id="colorcontentwrap8line">
    <div id="colorcontenth3_small" class="custom8"><a href="#" title="">{% for c in categorieblock10 %}
                {{ c.nom }}
            {% endfor %}</a></div>
    <div id="colorcontentwrap8">
        <h3><a href="{{ articlesblock10big.titre }}" rel="bookmark">{{ articlesblock10big.titre }}</a></h3>
        <a href="#"><img width="320" height="190" src="{{ asset('uploads/img/')}}{{ articlesblock10big.image }}" class="attachment-colorcontentblock10 " alt="" /></a>

        {{ articlesblock10big.titre|striptags[:150]|raw }}..
        <div class="cb"></div>
    </div>
    {% for article in articlesblock10 %}
        <div id="colorcontentwrap8">
            <h2><a href="{{ article.titre }}" rel="bookmark">{{ article.titre }}</a></h2>
            <div class="cb"></div>
        </div>
    {% endfor %}
</div>
